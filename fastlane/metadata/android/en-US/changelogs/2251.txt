The store search now runs only when you tap search, so no search request is sent while you type, and each query goes to the store's search provider (Algolia) with its analytics and personalization switched off. The app listing names Algolia.
